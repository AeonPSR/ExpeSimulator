{
  "name": "aeons-lab",
  "version": "1.2",
  "description": "An ensemble of tools designed to simplify and simulate several aspects of eMush's mecanics.",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "prepare": "git config core.hooksPath .githooks || exit 0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
