{
  "name": "expedition-simulator",
  "version": "2.0.0",
  "description": "Chrome extension for simulating planet expeditions",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
